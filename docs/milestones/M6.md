# M6 证据报告

- 日期：2026-08-31（实现）/ 2026-09-01（门禁关闭，Asia/Shanghai）
- 范围：libmosquitto MQTT QoS 1、batch、单 in-flight、匹配PUBACK和subscriber seq验证
- 实现状态：**已完成（Ubuntu x86_64与ARMv7目标）**
- 退出门禁：**2026-09-01 MET**
- M7及后续阶段：**未开始**

## 前置门禁

开始M6前在 `artifacts/20260831T134104+0800-m6-preflight/` 复核M5。起点提交为
`ae4fd3fdff908eab5112a065e6e3cc23d88f8284`；创建本run前工作区干净。warning-clean
构建PASS。沙箱内CTest唯一失败为既有PF_CAN权限限制，获准在沙箱外运行同一套测试后
12/12 PASS，M5 `test_pipeline`也通过。因此M5门禁保持MET，本轮用户请求构成进入M6
的明确授权。

## 实现

### libmosquitto和状态机

新增 `gateway_mqtt_sink`，直接链接libmosquitto，不手写MQTT协议。连接使用clean session，
发布固定为QoS 1，并调用 `mosquitto_max_inflight_messages_set(..., 1)` 限制库内最多一个
in-flight消息。实现同时在自身snapshot中保存当前MID，确保在前一个batch确认前不会
发布下一个batch。

publish调用分三层统计：调用前增加attempt，libmosquitto接受后增加accepted，只有
publish callback带回与当前in-flight完全相同的MID时才增加matched PUBACK并推进
batch_seq、records_acked和last acknowledged seq。其他MID增加unexpected计数；连接、
network loop、编码和PUBACK timeout错误进入独立MQTT error计数。

M6使用 `mosquitto_loop()` 驱动基线网络处理，没有使用M8计划的
`mosquitto_socket()`/`mosquitto_loop_read()`/`mosquitto_loop_write()`/
`mosquitto_loop_misc()` external reactor组合。

### batch格式与边界

payload是紧凑JSON，schema固定为 `gateway.telemetry.v1`，包含：

- `device_id`、`batch_seq`、`record_count`、`first_seq`、`last_seq`；
- 每条记录的gateway `seq`、`timestamp_ns`、CAN ID、DLC、8字节CAN data、ECU counter、
  status flags和32字节decoded payload；
- 二进制字段使用固定长度小写十六进制，batch内seq必须严格递增。

生产默认 `batch_interval_ms=1000`，固定最多256条记录，payload buffer固定128 KiB。
固定上限避免输入速率异常或interval配置较大时无界增长。上游queue drop造成的seq gap
允许保留并由subscriber观察；重复或非单调seq被拒绝。

### 定时、pipeline和退出

M5 ring buffer新增timed pop API，pipeline新增可选consumer idle callback。只有M6 MQTT
路径配置50 ms idle poll；M5 mock sink仍使用原来的无限等待pop。MQTT idle callback运行
libmosquitto loop并检查单调时钟batch deadline，所以只有一条稀疏记录、没有下一条输入
时也会发送到期batch。

`gatewayd --run-mqtt`复用M5 CAN producer、DBC decode、queue和self-pipe生命周期。
收到SIGINT/SIGTERM后关闭queue、producer退出、consumer drain已入队记录，然后flush
最后一个batch并等待匹配PUBACK，所有线程join后才销毁资源。配置新增
`mqtt_ack_timeout_ms`，默认5000 ms、范围100～60000 ms。

### 明确未实现的M7/M8行为

M6没有spool文件、cursor、CRC、断线重连、补传、崩溃恢复或subscriber去重。连接或
PUBACK失败会使consumer失败并走现有stop/close/join路径；未确认batch不会计作成功，
但只存在内存中且进程退出后不可恢复。该行为是M6边界，不是M7持久化语义。

## 测试结果

### Ubuntu warning-clean主机

最终run：`artifacts/20260831T135537+0800-m6-host-final/`。

- Ubuntu 22.04 x86_64、GCC 11.4.0、C11；
- `-Wall -Wextra -Wpedantic -Werror` fresh build PASS；
- 使用实际libmosquitto 2.0.11开发文件和动态库；
- 沙箱内CTest 12/13，唯一失败是PF_CAN socket创建被环境拒绝；
- 沙箱外相同CTest 13/13 PASS，M6 `test_mqtt_sink` PASS；
- batch编码、buffer不足、非单调seq、实际库加载、配置边界和ring timed pop均有测试。

开发过程run `artifacts/20260831T135039+0800-m6-dev-build/`、
`artifacts/20260831T135207+0800-m6-dev-regression/` 保留原始PASS/环境失败结果，但最终
主机结论只引用上述final run。

### Sanitizer

`artifacts/20260831T135603+0800-m6-asan-ubsan/` 使用ASan+UBSan fresh build，沙箱外
全量CTest 13/13 PASS，日志没有sanitizer报告。

LeakSanitizer：**NOT RUN**。当前执行环境受 `ptrace` 约束，因此显式设置
`detect_leaks=0`；不能从本run得出无泄漏结论。

### 实际Broker和1000-batch功能门禁

项目所有者先明确批准启动临时loopback Broker/subscriber和执行1000-batch测试。最终run：
`artifacts/20260831T135630+0800-m6-mqtt-final/`。

测试使用从Ubuntu仓库下载到 `/tmp`、未系统安装的Mosquitto/libmosquitto 2.0.11。
依赖包SHA256已归档。Broker配置只监听 `127.0.0.1:18884`、允许匿名测试连接、禁用
持久化；没有修改系统Broker、`/etc`、网络接口或系统服务。测试结束后以SIGINT停止临时
Broker，并确认18884端口不再监听。

1000-batch publisher结果：

```text
library=2.0.11
batches=1000
records=1000
publish_attempts=1000
publish_accepted=1000
puback_matched=1000
puback_unexpected=0
last_batch_seq=1000
last_gateway_seq=1000
failed=0
```

subscriber保存1000行、336027字节原始JSON。独立Python validator读取全部原始payload并
得到：raw_batches=1000、unique_batch_seq=1000、unique_gateway_seq=1000，batch_seq和
gateway seq都严格为1～1000，missing=0、duplicate=0、reordered=0。Broker原始日志又
独立统计final topic Received PUBLISH=1000、发送给publisher的PUBACK=1000、转发给
subscriber的PUBLISH=1000。

这1000个batch各含1条测试记录，用来快速覆盖1000次publish/PUBACK状态转换，不能解释
成1000秒生产运行、111帧/s输入、吞吐率或时延测试。

### 低流量batch timer

同一final run另有单独topic和subscriber。用例设置测试专用 `batch_interval_ms=100`、
最大256条，只放入1条记录且不再输入；idle poll触发publish并收到匹配PUBACK：

```text
interval_ms=100 elapsed_ms=105.236 batches=1 records=1
puback_matched=1 buffered=0 failed=0
```

105.236 ms只证明本run中deadline路径生效，不是产品时延或性能数字。生产默认interval
仍为1000 ms。

### 首次ARMv7依赖审计和真实目标缺口

`artifacts/20260831T135759+0800-m6-arm-dependency-audit/` 使用现有Buildroot GCC 7.5.0
SDK尝试配置M6。编译器和pthread检测成功，但CMake退出1，因为SDK sysroot没有
`mosquitto.h`、目标 `libmosquitto.so`/`.a` 或 `libmosquitto.pc`。

- 该run的ARMv7交叉构建：**NOT RUN**；
- 该run的M6 ARM binary生成和SHA256：**NOT RUN**；
- 该run的i.MX6ULL部署/动态加载：**NOT RUN**；
- 截至该run的真实物理CAN → decode → queue → MQTT：**NOT RUN**；
- 截至该run的局域网跨主机Broker/subscriber：**NOT RUN**；
- 该run的目标libmosquitto精确版本/API验证：**NOT RUN**。

历史板端审计只看到 `/usr/lib/libmosquitto.so(.1)`，不能代替版本匹配的头文件和可复现
交叉链接输入，也不能把x86_64主机库用于ARM构建。以上是该依赖审计run的真实历史结果；
后续私有构建和板端证据单独记录，不倒写本run。

### 收尾审计

`artifacts/20260831T140625+0800-m6-close-audit/` 在不重新启动Broker的前提下：

- 重跑M6专项CTest 1/1 PASS；
- 对已归档的1000条subscriber JSON重放validator，结果仍为1～1000连续且
  missing/duplicate/reordered均为0；
- 复核18个归档源码/测试hash和3个x86_64二进制hash，全部一致；
- 复核9个JSON文件、Python validator语法、`git diff --check` 和M7/M8越界关键字；
- 沙箱内端口检查因netlink权限受阻，随后以获批准的只读非沙箱检查确认
  18884无listener，临时Broker已停止。

首次validator重放误用两个脚本不支持的参数而退出2；原始失败日志与修正后
PASS日志均保留，没有覆盖证据。这是收尾命令输入错误，不是实现或payload失败。

### i.MX6ULL到Windows Broker局域网1000-batch

经项目所有者明确批准的`artifacts/20260831T220718p0800-m6-lan-1000/`在真实i.MX6ULL
上运行SHA256固定的ARMv7 binary，动态加载私有libmosquitto 2.0.11，以真实物理CAN输入
连接专用有线LAN内的Windows Mosquitto 2.1.2。正式subscriber退出码为0、stderr为空，
保存1000行原始JSON、共115335条record；严格validator结果为：

```text
raw_batches=1000
batch_seq=1..1000
gateway_seq=1..115335
missing=0 duplicate=0 reordered=0
```

gateway收到停止信号后退出0，最终publish attempt/accepted/matched PUBACK、
batches_acked均为1033，records_acked为119082，unexpected PUBACK、MQTT error和queue
drop均为0。Broker原始日志独立计数为板端PUBLISH/PUBACK 1033/1033、正式subscriber
PUBLISH/PUBACK 1000/1000，与gateway summary一致。subscriber在第1000批退出后gateway
继续确认33批，因此1033与1000的差异不是subscriber门禁内缺失。

CAN前后均为ERROR-ACTIVE、500 kbit/s，berr tx/rx为0；RX packets/bytes增量为
119103/952824，RX error/drop/overrun及CAN restarted、bus-errors、arbit-lost、
error-warn、error-pass、bus-off增量均为0。CAN快照窗口比gateway进程边界宽20帧，不据此
推测丢包或性能。gateway summary中`can_accepted=119083`比`queue_success=119082`多1帧，
停止边界原因没有独立证据，因此不声称每个CAN输入都已发布。

### Ubuntu独立门禁复核

2026-09-01在Ubuntu复核上述LAN run的完整原始证据：

- 原始`manifest.sha256`为Windows CRLF清单；GNU工具直接读取会把`\r`当作文件名字符。
  只读去除清单行尾`\r`后，131/131个文件hash全部PASS，未修改原始证据；
- 当前严格整数validator与原始测试提交无Git内容差异；按Windows CRLF字节重算的脚本
  hash与归档值一致，validator回归8/8 PASS；
- 独立重放24.5 MB subscriber原始JSON，1000批及115335条seq连续性再次PASS；
- 直接从Broker、gateway和CAN原始日志重新计数，结果与manifest、Windows侧报告和
  machine-readable summary一致。

### 保留限制

- 板端wall clock为1970：正确UTC、timestamp correctness和端到端时延 **NOT RUN**；
- Broker精确退出码 **NOT AVAILABLE**，只记录正常终止日志及最终无listener；
- 即时板端`pgrep`不可用，稍后`ps`/`/proc`补证为0进程；
- 吞吐、性能、长期可靠性、断线重连、spool、补传、恢复、去重、`kill -9`和epoll
  **NOT RUN**。

## 退出判定

M6源码、主机warning-clean/ASan+UBSan、实际libmosquitto、单in-flight QoS 1状态机、
真实ARMv7目标运行、i.MX6ULL物理CAN到局域网Broker、至少1000-batch匹配PUBACK和
subscriber seq验证均有真实证据。Ubuntu独立复核确认manifest、validator及
Broker/gateway/CAN原始计数一致，因此M6总退出门禁为 **2026-09-01 MET**。

该结论只覆盖M6 QoS 1功能基线，不能写成正确UTC、端到端时延、吞吐、长期可靠性或
持久化结果。M7 spool/重连/恢复、M8 epoll、M9部署和M10性能/稳定性仍未测试或实现；
进入M7需要项目所有者在M6关闭后另行明确授权。
