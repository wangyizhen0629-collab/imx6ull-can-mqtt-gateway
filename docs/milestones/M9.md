# M9：BusyBox部署与进程恢复

## 当前状态

- 前置M8门禁：**MET**
- BusyBox inittab/foreground supervisor方案：**PASS**
- 主机warning-clean / 全量CTest / M9专项：**PASS（18/18；1/1）**
- ASan+UBSan全量CTest：**PASS（18/18）**
- ARMv7 warning-clean交叉构建/ELF/RPATH：**PASS**
- 真实i.MX6ULL BusyBox 1.31.1 PID 1只读身份：**PASS**
- M9 binary/目标库板端SHA硬门禁、备份、`/opt`和四个授权`/etc`安装：**PASS**
- PID 1 HUP启动、唯一supervisor/child、受控restart和异常拉起：**PASS**
- 目标BusyBox 1.31.1 ash隔离重启风暴cooldown：**PASS**
- 唯一一次reboot后的BusyBox init自动拉起supervisor：**PASS**
- 最终唯一supervisor/child、身份和超过60秒稳定状态：**PASS**
- M9总退出门禁：**MET**
- M10及后续：**未开始**

M9只增加BusyBox兼容部署和进程恢复，不实现M10压力、性能、`/proc`指标或24小时测试。
主机上的supervisor功能测试不能替代目标板PID 1、真实`/etc/inittab`和reboot证据。

## 前置门禁

`artifacts/20260901T175107+0800-m9-preflight/`在开始修改前确认HEAD与`origin/master`均为
`bf66961e0f8b600fb10d18c72142be25d449e1c0`。M8最终validator为exit0：323个raw batch、
51523条raw record、24089条raw duplicate，去重后27434条seq连续，missing和effective
duplicate均为0；最终artifact manifest 201项无差异。因此M8为`MET`，M9获得本轮用户
授权，M10没有开始。

## 实现与设计边界

1. `deploy/init.d/gatewayd`是POSIX/BusyBox ash脚本。BusyBox init通过
   `null::respawn:/etc/init.d/gatewayd supervise`启动前台supervisor；脚本再启动唯一的
   `gatewayd --config ... --run-mqtt`子进程。脚本不命名为rcS自动扫描的`S??gatewayd`，
   避免rcS和inittab重复启动。
2. `deploy/config/gatewayd.env.example`只保存binary/config/run目录和节流参数；真实MQTT
   凭据仍在私有`gateway.conf`，不提交源码。binary、config和run目录必须是绝对路径。
3. 子进程无论正常或异常退出都会重新拉起。连续快速退出达到
   `GATEWAYD_RESTART_LIMIT`后等待`GATEWAYD_COOLDOWN_SEC`；运行达到
   `GATEWAYD_STABLE_SEC`后清零快速退出计数。
4. `restart`向经`/proc/<pid>/cmdline`核验的supervisor发送HUP，由supervisor向子进程
   发送TERM并等待退出，再启动不同PID；该受控重启不计入故障风暴。
5. `stop`在`/var/run`创建临时disabled标记并停止子进程，supervisor保持存活，避免init
   立即respawn；`start`移除标记并恢复。init终止supervisor时TERM会转发给子进程并清理
   PID/lock文件。
6. `deploy/tests/test_gatewayd_supervisor.sh`使用仅属于测试的fake gateway和临时目录验证
   以上状态机。CMake在`BUILD_TESTING`启用时要求主机BusyBox并注册M9标签测试。

选择BusyBox `inittab`而不是未知的镜像外部supervisor，是因为目标规范已确认无systemd，
而BusyBox init是已知基线。该选择仍须在真实目标BusyBox 1.31.1上验证，不能只凭脚本和
Ubuntu BusyBox 1.30.1关闭门禁。

## 主机与sanitizer证据

主机run为`artifacts/20260901T175412+0800-m9-host-final/`：

- 首次CMake因默认路径没有libmosquitto开发文件而exit1；失败输出保留。第二次显式复用
  M6～M8的x86_64私有依赖后配置exit0，warning-clean构建exit0。
- 受限沙箱全量CTest为16/18，M9专项已经PASS；既有`test_can_receiver`无法创建PF_CAN
  socket，`test_mqtt_sink`无法保留TCP socket，均在descriptor断言处失败。相同binary在
  沙箱外重跑为18/18、M9 1/1，exit0；首次结果没有覆盖。

ASan+UBSan run `artifacts/20260901T175610+0800-m9-asan-ubsan/`使用
`detect_leaks=0:halt_on_error=1`和UBSan halt/stacktrace，沙箱外全量18/18、M9 1/1，
exit0。LeakSanitizer因当前执行环境由ptrace管理而为`NOT RUN`，没有写成PASS。

详细M9脚本run为
`artifacts/20260901T175900+0800-m9-busybox-supervisor-final/`。Ubuntu BusyBox 1.30.1
ash实际执行异常退出42后自动重拉；受控restart把测试子PID 34替换为58；stop后1秒内
start次数不增加，start后恢复；连续3次快速失败触发`event=restart_storm`，1秒观察窗
没有第4次启动，2秒cooldown后恢复。完整239行shell trace、输入SHA256和exit0均已保存。
PID只是该临时主机run的事实，不代表目标板PID或性能数值。

在正式artifact前曾直接执行两次开发期BusyBox命令：第一次虽最终PASS，但暴露BusyBox
`grep`不支持大写`-R`；修正为`-r`后第二次直接执行PASS。这两次输出没有进入唯一artifact，
违反证据契约，因此明确不用于任何门禁结论；正式结论只引用上述后续唯一run。

## ARMv7交叉构建

`artifacts/20260901T175700+0800-m9-arm-cross/`保留全部尝试：

1. 首次把SDK写成相对路径，CMake找不到完整编译器路径并exit1；
2. 改用绝对路径后warning-clean构建成功，但审计发现构建机绝对RPATH，明确记为FAIL；
3. 增加`CMAKE_SKIP_RPATH=TRUE`重新配置和构建，最终warning scan、ELF和RPATH检查均exit0。

最终binary SHA256为
`6e8729417b3dc40c10a413459de5eca9be43ce58dfcc8a3b12e91f5c8d7ef958`，是ARM EABI5
hard-float ELF32，解释器`/lib/ld-linux-armhf.so.3`，依赖`libpthread.so.0`、
`libmosquitto.so.1`和`libc.so.6`，无RPATH/RUNPATH。它没有部署或在目标板执行，因此该
结果不能描述为目标动态加载或服务启动PASS。

## 真实板端门禁与退出判定

`artifacts/20260901T180003+0800-m9-board-not-run/test_report.md`将以下项目全部标为
`NOT RUN`：目标文件部署、`/etc`备份/安装、inittab reload或reboot、真实开机自动启动、
受控restart、一次目标进程异常终止后的拉起，以及目标BusyBox 1.31.1重启风暴冷却。

原因是当前Ubuntu会话没有目标板登录端点、凭据或已验证的传输路径，不能实际访问
i.MX6ULL；真实地址/凭据也不得从私有历史证据复制进仓库。所需条件是使用项目所有者已
验证的MobaXterm或提供Ubuntu到目标板路径，并在操作前明确批准本次指定`/etc`、init、
reboot和进程控制范围，随后保存脱敏的原始板端证据。

M9的源码、主机、sanitizer和ARM交叉构建均已有真实PASS证据，但计划要求的“开机启动
和异常拉起”没有真实目标板证据，所以M9总门禁只能是 **NOT MET**。本轮到此停止，
不进入M10。

## Windows真实目标续跑

`artifacts/20260901T182509+0800-m9-windows-board-gate/`已通过既有Windows SSH/
MobaXterm路径登录目标，并用`/proc/1`、`ldd /sbin/init`及目标库字符串只读确认：PID 1
为`/sbin/init`，链接`/lib/libbusybox.so.1.31.1`，库内含BusyBox 1.31.1、respawn和
`reloading /etc/inittab`语义。本镜像没有`/bin/busybox`多调用命令，而是standalone
applet；不能把命令缺失误判为非BusyBox。

指定Ubuntu构建文件无法通过Windows现有SSH认证读取；Windows clone和目标板只存在旧
M6～M8 binary，没有预期M9 SHA候选。因此本轮没有从要求的源文件重新计算SHA256，严格
在staging和目标写入前停止。`/etc/inittab`始终保持SHA256
`77676429d4b24e2a93b8d7f7c3a9a35a37785600ab359e1c75ec6c8da11d1380`；其他三个授权
M9文件仍不存在，未发送HUP、未reboot、未发进程信号。结束审计为supervisor/gatewayd
0/0且无M9 staging，这只证明没有遗留测试状态，不满足最终一/一服务门禁。目标时钟仍
为1970。部署、开机、restart、异常拉起、fake storm cooldown及最终服务状态全部保持
`NOT RUN`，M9继续`NOT MET`，M10未开始。

## Windows真实目标最终续跑

最终run为`artifacts/20260901T204152+0800-m9-windows-board-gate-final/`。开始前Windows
clone跟踪文件无改动，`git pull --ff-only origin master`为already up to date，HEAD为
`17d698e16d5cfe8e86fa0b4fdc47ea9515df65ab`并包含要求的基线提交。1305个既有未跟踪
文件全部保留，本轮没有覆盖或删除历史artifact。

### 输入、备份与安装

- 板端`/tmp/m9-staging/gatewayd`重新计算为104860 bytes、SHA256
  `6e8729417b3dc40c10a413459de5eca9be43ce58dfcc8a3b12e91f5c8d7ef958`；ELF32 ARM
  EABI5 hard-float、解释器`/lib/ld-linux-armhf.so.3`，NEEDED为`libpthread.so.0`、
  `libmosquitto.so.1`、`libc.so.6`，无RPATH/RUNPATH。
- 目标已有libmosquitto 2.0.11真实文件SHA256为
  `b32c8ac4defb2b2920fba2e42f263869508c42e3c1719440db37ffc8d8c2f636`；以其目录执行
  `ldd`全部解析且`gatewayd --version` exit0。没有覆盖系统`/usr/lib`版本。
- 写入前`/etc/inittab`为root:root 0644，SHA256
  `77676429d4b24e2a93b8d7f7c3a9a35a37785600ab359e1c75ec6c8da11d1380`；其他三个
  授权M9文件和`/opt/gatewayd`均不存在，supervisor/child为0/0。根目录、`/opt`和
  `/var/lib`均位于`/dev/root` ext4。
- 首版本地私有配置生成器造成三个重复key，被独立计数发现并保留为FAIL，未传到目标。
  v2严格确认14个key各一次，保持CAN/Broker/凭据/队列/超时值不变，仅为本run更换
  device/topic/spool。
- 原inittab及“原文件不存在”标记、可恢复rollback脚本保存于本轮ext4目录。安装后
  binary root:root 0755、库0644、init脚本0755、env0644、私有config0600；唯一inittab
  行为`null::respawn:/etc/init.d/gatewayd supervise`，没有`S??gatewayd`。安装后inittab
  SHA256为`fef0324868e70582fef1d76c8dd3bcd6025ae2159ba383a8585dc091d9dfad84`。

### reload、restart、异常恢复与cooldown

PID 1 HUP exit0后，supervisor PID 11337和子PID 11348在10秒观察内不变；脚本/config/
binary SHA、父子关系、完整cmdline、子exe及`/opt/gatewayd/lib`映射全部匹配。首版计数
把shell supervisor的`comm=gatewayd`也当成binary而exit1；`ps`已显示实际只有一条shell
supervisor和一条ARM binary，后续以cmdline分类supervisor、以exe+SHA+cmdline+PPID分类
child，得到1/1及不相关gatewayd为0。该首版FAIL没有被覆盖。

只对核验后的子PID 11348执行一次SIGKILL，命令exit0；同一supervisor 11337自动拉起
子PID 14335，5秒后仍为1/1。随后`/etc/init.d/gatewayd restart` exit0；脚本的TERM转发
路径和子进程已捕获SIGTERM位均已记录，旧子PID 14335消失，新PID 15095稳定，同一
supervisor保持且没有不相关gatewayd。

storm测试没有使用真实gatewayd。目标`/bin/ash`和`libbusybox.so.1.31.1`身份、SHA及
BusyBox 1.31.1版本已保存；仓库supervisor/fake仅在隔离副本把shebang改成`/bin/ash`，
其余body SHA完全相同，仓库test脚本保持原hash。完整ash `-x` trace证明第3次快速失败
后`event=restart_storm`出现，1秒窗口启动数保持3，2秒cooldown后才出现第4次并恢复。
测试临时目录和进程均为0，生产PID 11337/15095测试前后不变。

### reboot阻塞与门禁判定

reboot前持久化marker保存boot ID、PID、inittab及binary hash，服务status exit0；板端
wall clock仍为1970，不能解释为正确UTC。CAN只读快照为UP/ERROR-ACTIVE、当前berr 0/0，
但保留历史累计error-warn/error-pass，未修改接口。配置的Broker连接只观察到SYN-SENT；
本轮没有启停或修改Broker，也没有产生MQTT交付结论。

唯一一次`sync; /sbin/reboot`已发出。首次Windows控制器在板端断线后的预期SSH错误处
因PowerShell原生命令stderr终止，故保持FAIL；为避免第二次reboot，恢复脚本明确不含
reboot命令，只执行48次SSH探测。探测从本地21:14:58到21:22:51全部exit255，未读取到
新boot ID。首版公开脱敏又发现PowerShell缩写IPv4残片，原字节移入Git忽略的private
quarantine，v2对精确endpoint和2～4段点分数字复扫为0泄漏。

因此不能把“reboot命令已发送”或“SSH断线”写成开机成功。真实开机自动启动、post-boot
唯一supervisor/child、最终服务状态、post-boot CAN/Broker及需要时的rollback均为
**NOT RUN**。目标登录不可用时不得修改网络、重复reboot或猜测地址。M9六项总门禁中
开机和最终状态缺证，结论只能是 **NOT MET**；M10没有开始。

## 手动post-boot补充门禁与最终判定

补充run `artifacts/20260901T230215+0800-m9-manual-postboot-gate/`由操作者手动执行板端
命令，Codex只提供指导。新boot ID为`0abefcf0-9d85-4a4b-b335-f339b33b8db4`，与reboot前
不同；操作者确认首次检查前未人工start/restart/HUP。唯一supervisor PID 337、PPID 1、
cmdline匹配`/etc/init.d/gatewayd supervise`，因此BusyBox init开机自动拉起supervisor
为PASS。部署文件权限和SHA均与基础run一致。

重启后`can0`初始DOWN/STOPPED、统计为0，supervisor存在但child为0。该状态不写成服务
PASS，也不武断断言CAN是唯一退出原因。为避免真实binary继续周期尝试，操作者受控stop
并得到disabled、1/0、无其他/测试进程。随后操作者新增明确授权，只按仓库既有基线恢复
`can0`为500000 bit/s、loopback off、UP；恢复及最终状态均为UP/LOWER_UP、ERROR-ACTIVE、
berr 0/0，没有修改Broker、网络配置或STM32。

最终受控start exit0；5秒status running exit0，supervisor/child为337/9951。超过60秒后
status仍exit0，child PID仍为9951；child PPID、exe、完整cmdline、预期binary SHA和固定
libmosquitto映射均通过，最终1 supervisor/1 gatewayd、其他gatewayd 0、测试进程0，且
disabled不存在。

基础run已通过受控restart换PID、一次子进程SIGKILL自动拉起和目标BusyBox 1.31.1 ash
隔离fake快速失败cooldown。本补充run补齐真实reboot/init和最终状态，六项门禁均有真实
证据，因此M9总门禁为 **MET**。板端时钟仍为1970；CAN持久自动配置、Broker交付、正确
UTC、完整无人值守产品ready、性能及可靠性均不由该结论支持。M10没有开始。
