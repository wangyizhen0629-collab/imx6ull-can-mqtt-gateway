# M7：持久化 spool、重连补传与崩溃恢复

## 当前状态

- 前置 M6 门禁：**MET**
- M7 源码实现、Ubuntu 离线测试、ASan+UBSan、ARMv7交叉构建：**PASS**
- Windows Broker断线/恢复、subscriber原始证据、`SIGKILL`恢复：**PASS**
- i.MX6ULL真实物理CAN与目标ext4持久目录测试：**PASS WITH LIMITATIONS**
- tail/internal/state损坏恢复：**PASS**
- M7 总退出门禁：**MET**
- M8及后续阶段：**未开始**

项目所有者明确批准了本次专用Windows Broker/subscriber、目标持久测试目录、gateway
生命周期、一次核实PID的`kill -9`及专用spool损坏注入。真实LAN地址、用户名和原始配置
留在Git忽略的`private_raw/`，提交副本已脱敏并用SHA256绑定。本记录只关闭M7，不批准
M8 external-loop/epoll或后续阶段。

## 开始前门禁

`artifacts/20260901T093205+0800-m7-host-pre-broker/` 记录以下事实：

1. 起始 HEAD 为 `f185222bc685e54355b635388bf0094f7ec41b6e`，开始时本地与
   `origin/master`一致。
2. 已完整阅读根目录 `AGENTS.md`、`docs/PROJECT_SPEC.md`、`docs/PLANS.md` 和
   `docs/milestones/M6.md`。
3. `docs/PLANS.md`和M6里程碑均明确M6于2026-09-01达到门禁；项目所有者随后单独批准
   只进入M7。
4. 本阶段没有实现或调用M8 external-loop/epoll API。
5. Windows跨主机接续开始前先保护未提交内容并执行`git pull --ff-only origin master`；
   HEAD与`origin/master`均为`fefd07558f944f7cc1b2527730bae75ddd891be5`，且该要求提交的
   ancestor检查exit0。后续门禁只增加文档和证据，没有修改生产源码。

## 已实现的M7语义

### spool格式与耐久边界

- `spool.data` 是append-only文件。每条固定80字节：4字节magic、版本、entry长度、
  CRC32、保留字段和显式little-endian编码的64字节`telemetry_record`载荷；不直接把C
  结构体内存写盘，避免ABI/padding/端序隐含依赖。
- 每次append完成后调用`fdatasync`，返回成功才允许consumer接收下一条；磁盘写入或
  sync失败会停止pipeline并计入独立spool error，不把记录伪装为已持久化。当前原型
  不丢弃旧记录、不压缩文件，也没有容量阈值；ENOSPC等错误采用fail-stop。
- `spool.state` 固定40字节，保存CRC、已确认byte offset、最后确认gateway seq和下一个
  batch seq。推进时先写`.tmp`并`fdatasync`，再`rename`并`fsync`父目录。
- `spool.data`使用非阻塞独占`flock`，防止两个`gatewayd`同时竞争同一个游标；进程被
  `SIGKILL`后锁由内核释放。编译启用`_FILE_OFFSET_BITS=64`。

### 恢复与确认语义

- 启动时逐条校验magic/version/length/CRC和严格递增seq。部分尾部或最后一条损坏会截断
  到最后有效entry并同步；内部损坏会拒绝启动，禁止跨过未知数据继续发布。
- state缺失或CRC/offset/seq不一致时安全回退到offset 0和batch 1。该策略可能产生原始
  MQTT重复，但不会错误宣称未确认记录已经交付。
- 只有匹配当前MID的PUBACK到达后才原子推进state。连接丢失、PUBACK超时或进程在state
  落盘前退出时，prepared batch会从原游标重放。
- 新CAN记录的`gateway_seq`从`spool.data`最后有效记录恢复，不从state已确认游标恢复，
  避免离线积压后重启复用seq。
- durable模式下初始连接失败和运行中断线不会令consumer丢弃记录；记录继续落盘，按
  `mqtt_reconnect_interval_ms`重试。每次重连重建clean-session客户端，最多保持一个
  in-flight QoS 1 batch。
- 正常退出时若Broker仍离线，已成功同步到spool的pending记录允许保留到下一次启动；
  summary明确报告pending，而不计作PUBACK成功。

### validator与统计

- `validate_m7_recovery.py`逐条复用M6严格JSON schema检查；允许QoS 1原始重复，但要求
  同一`device_id + seq`的重复内容完全一致，并要求第一次出现的unique seq连续有序。
- summary分别报告raw records、raw duplicate、unique seq、missing和去重后的effective
  duplicate；不把原始重复描述成数据丢失。
- stats新增spool append/replay/ACK、tail recovery、state recovery、corruption和error，
  与MQTT transport error分开。

## 已执行的真实测试

最终离线证据为`artifacts/20260901T093654+0800-m7-offline-final/`：

- Ubuntu GCC 11.4.0 Debug warning-clean构建PASS；沙箱外全量CTest **16/16 PASS**，
  M7标签 **3/3 PASS**。
- 受限沙箱首轮全量CTest为15/16，唯一失败是既有M2测试无法创建`PF_CAN` socket；该失败
  已保留。沙箱外同一全量套件16/16通过。
- ASan+UBSan全量CTest **16/16 PASS**；`detect_leaks=0`，所以LeakSanitizer为
  **NOT RUN**。
- `test_spool`覆盖append/sync、ACK state、重开、尾部恢复、state损坏安全回退、内部
  损坏拒绝和单写者锁。恢复validator回归 **5/5 PASS**。
- Buildroot GCC 7.5.0 ARMv7 warning-clean交叉构建PASS；最终binary SHA256为
  `9c4efe5c12f9797e8eda03ada8c6b2162ff0225aa7680c7ac199afdc45382b25`，是ARM EABI5
  hard-float ELF32，依赖目标`libmosquitto.so.1`且无RPATH/RUNPATH。

离线恢复驱动器`test_mqtt_recovery_driver`及显式脚本
`tools/mqtt/run_m7_recovery_integration.py`已构建/完成语法检查，但没有实际启动。脚本只
适合Ubuntu本机loopback临时Broker；本项目实际Broker位于Windows，所以Windows接续
测试应按下节保存跨主机原始证据，而不是把未运行的脚本写成结果。

## Windows/i.MX6ULL退出证据

最终跨主机run为`artifacts/20260901T105414+0800-m7-lan-recovery-gate2/`：

1. Windows使用Mosquitto 2.1.2专用端口18884；三个Broker实例的gateway PUBLISH/PUBACK
   分别为19/19、116/116、146/146，总计281/281。Broker到subscriber的PUBLISH/PUBACK
   也为281/281。配置、监听、启停命令和完整脱敏日志已保存。
2. 板端重新计算部署文件：gateway SHA256为
   `9c4efe5c12f9797e8eda03ada8c6b2162ff0225aa7680c7ac199afdc45382b25`；
   实际libmosquitto 2.0.11 SHA256为
   `b32c8ac4defb2b2920fba2e42f263869508c42e3c1719440db37ffc8d8c2f636`。
   `ldd`解析到专用`/tmp/m7/lib/libmosquitto.so.1`。spool使用`/dev/root` ext4上的专用
   `/var/lib`目录，最终可用687 MiB；`/tmp` tmpfs不作为耐久证据。
3. phase1在线交付seq1～4864。Broker停止后gateway PID1706继续运行，cursor保持seq4864，
   spool增长到34309条，形成29445条pending；离线稳定窗口内state hash不变。
4. 对核实PID1706仅执行一次`kill -9`，命令返回0、wait137；前后spool大小、data/state
   hash完全相同。PID1926使用同binary/config/spool重启，按seq4865～34309补传且没有
   seq复用。随后同一PID又经历一次实际Broker断线/恢复，summary记录`reconnects=1`。
5. 物理CAN恢复窗口新增1335条并全部ACK。phase2 summary为publish/PUBACK122/122、
   unexpected0、queue drop0、spool append/replay/ACK/pending为1335/30780/30780/0，
   spool corruption/error均0。
6. 尾部offset2851520追加`de ad be ef`后只截断4字节且恢复原有效前缀hash；内部
   offset80000破坏magic后gateway exit1并在MQTT connect前拒绝；state offset0损坏后
   `spool_state_recoveries=1`并从头重放35644条，140/140匹配PUBACK、pending0。
7. 最终validator自测5/5；实际捕获使用期望seq1～35644和`--require-raw-duplicates`
   得到raw batch/record281/71288、raw duplicate35644、unique gateway seq35644、
   missing0、effective duplicate0。
8. 板端未压缩tar为11585024 bytes，SHA256
   `51bd1b5c4197c2703ebd10478bd9b39b2ac9b38b991046e95ce5e0dd2f30ae4b`；解包前路径
   检查通过，board manifest在本地逐文件复核115/115。run级`manifest.sha256`也实际
   验证通过。

两个早期run按失败保留：`20260901T100727+0800-m7-lan-recovery-final`出现
`queue_drop=1303`及subscriber missing309；`20260901T103725+0800-m7-lan-recovery-gate`
出现`queue_drop=72`。两者均正常停止、没有执行SIGKILL，不参与最终PASS判定。

## 限制、NOT RUN与停止边界

- phase1按门禁要求以SIGKILL结束，没有最终queue summary，所以其`queue_drop`直接计数为
  **NOT AVAILABLE**，不能填写为0；正常退出phase2/phase3均为0，gateway seq范围内
  missing为0。
- `can0`从ERROR-ACTIVE变为ERROR-WARNING，最终berr tx/rx为0/102；Linux RX errors/
  dropped为0/0，error-warn累计220→223，error-pass3→3，bus-off0→0。不能声称本次
  物理层错误计数为零。
- 目标wall clock仍未设置，板端日志为1970。正确UTC、补传时延、吞吐、CPU/RSS、
  spool寿命/容量上限、compaction、真实掉电恢复和长期可靠性：**NOT RUN**。
- LeakSanitizer：**NOT RUN**。
- M8 external-loop/epoll、M9部署和M10性能/稳定性：未开始、未批准。

断线/恢复和实际`kill -9`已证明顺序恢复，最终`missing=0`、
`effective duplicate=0`，因此M7状态为 **MET**。到此立即停止，不进入M8。

## M10 spool v2兼容边界（2026-09-02追加）

M7上述证据和格式结论仅对应legacy GSP1/GST1。M10 corrective prep保留该实现、默认选择和
`sync_records=1`逐条同步语义，不改写M7历史证据；新增v2必须显式配置并使用全新目录，
不对现有`spool.data`/`spool.state`做原地迁移、覆盖或删除。v2以分段回收和sequence
reservation替代单文件无限增长路径；真实板端掉电、介质寿命与长期容量行为仍为`NOT RUN`。
