# M3 证据报告

- 日期：2026-08-29（Asia/Shanghai）
- 范围：STM32 确定性模拟 ECU、断电物理层检查、物理 CAN 和有限真实接收
- 当前状态：**进行中，停在 M3-A；M3 退出门禁未满足**
- M4 及后续阶段：未开始

## 前置门禁核验

开始 M3 前只读核验了 M2 的最终 ARM 构建、冻结源码 checksum、板端原始归档和最终
逐字节审计。新证据为 `artifacts/20260829T141730+0800-m3-preflight/`：ARM binary 和
板端 tar 的 SHA256 均与 M2 记录一致，26/26 冻结源码条目匹配，M2 最终审计仍为 PASS。
因此 M2 门禁满足，允许只进入 M3。

该核验没有把 M2 controller loopback 外推为物理 CAN 或 STM32 结果。

## 当前 M3 状态

### M3-A：Windows CubeMX 基础工程

**NOT RUN - 需要用户在 Windows STM32CubeMX/Keil 中验证。**

当前执行环境是 Ubuntu 22.04 x86_64 虚拟机，没有 STM32CubeMX 或 Keil UV4。只读检查
还确认仓库当前只有 `stm32/README.md` 和 `stm32/firmware/.gitkeep`，没有 `.ioc`、
`.uvprojx` 或生成后的 STM32 源码。实际板卡晶振、Clock Tree、APB1、500 kbit/s bit
timing、PB8/PB9 Remap 和 Windows 工具版本仍没有真实记录。

M3-A 必须在 Windows clone 中完成并回传 `.ioc`、`.uvprojx`、工具版本和 Keil Build
输出。Ubuntu 缺少 STM32 工具链不是安装依赖的理由，也不能用手工伪造工程替代门禁。
Windows Codex 的完整交接提示词保存在 `docs/milestones/M3_WINDOWS_CODEX_PROMPT.md`。

### M3-B：STM32 确定性模拟 ECU

**NOT RUN - M3-A 尚未通过。** 为遵守顺序门禁，本轮没有提前创建固件业务源码，也没有
声称三类周期帧、Rolling Counter、XOR 或确定性信号已经实现/编译。

### M3-C：断电物理层检查

**NOT RUN - M3-A/M3-B 尚未通过，且需要真实硬件与万用表。** 本轮没有连接、上电或
测量 STM32、TJA1050 或 i.MX6ULL，CANH--CANL 实际电阻仍未知。

### M3-D：物理 CAN 与 candump

**NOT RUN - M3-C 尚未通过。** 本轮没有请求或执行 STM32 烧录，没有修改 `can0`，没有
启动物理 `candump`。后续烧录和接口状态修改仍需在执行前分别取得明确批准。

### M3-E：gatewayd 真实 CAN

**NOT RUN - M3-D 尚未通过。** 本轮没有让 `gatewayd` 接入物理 CAN，也没有执行至少
10 分钟的连续接收，因此不存在可报告的按 ID frame 数、counter gap 或 CAN error 数值。

## 本轮安全边界

- 只执行仓库和已有 artifact 的只读核验，并新增本次唯一 preflight artifact/文档。
- 没有修改硬件、固件、`can0`、网络、`/etc`、init、进程、Broker 或依赖。
- 没有创建 DBC、解码器、producer--consumer、MQTT、spool、epoll 或部署功能；M4+
  保持未开始。

## 当前退出判定

**NOT MET。** M2 前置门禁已通过，但 M3-A 尚无 Windows CubeMX/Keil 真实工程和 Build
证据。按 M3-A～M3-E 顺序门禁，当前必须停在 M3-A，不能把 M3 标记为完成。
