# M2 第一次最终证据审计

上传 tar 的 SHA256 检查通过，但 `tar -df` 同时比较了 UID/GID：归档来自板端
`root/root`，主机以非特权用户解包，因此报告所有文件所有权不同。验证脚本由管道交给
`tee` 后，shell 又取得了 `tee` 的 0 而不是脚本的非零退出码。

本 run 必须保留为 **FAIL**。失败属于主机证据核验方法，不推翻或确认板端功能结果；
后续必须新建 run，逐一比较归档成员内容字节，并直接捕获验证脚本退出状态。
