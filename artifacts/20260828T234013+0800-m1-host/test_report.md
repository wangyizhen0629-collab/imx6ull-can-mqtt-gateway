# M1 中间主机测试报告

warning-clean Debug 构建和 CTest 8/8 PASS，配置文件 + CLI 覆盖的直接运行退出 0，日志中
密码为 redacted。随后又整理了版本元数据、中文并发约束注释和测试临时文件关闭路径，
因此本 run 保留为中间结果，M1 最终门禁引用新的 final run。

ARMv7、i.MX6ULL、CAN、STM32、MQTT、spool、部署和性能均为 **NOT RUN**。
